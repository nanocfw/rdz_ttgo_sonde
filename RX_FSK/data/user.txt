# Username,Level,Password
# Level 0: No access (anonymous: only Home / Data / Livemap and the login page)
# Level 1: View QRG/Config and use the Control tab; cannot save config changes
# Level 2: Full access (WiFi, Users, saving config, firmware update, everything)
# The empty-username line below sets the access level for unauthenticated clients.
# A fresh device ships ",2," (open) so the first user can be created from the "Users" tab.
# Creating that first account automatically rewrites this line to ",0," so anonymous users
# lose config access. User management is open only until the first account exists; after
# that it requires logging in as a level-2 user.
,2,
